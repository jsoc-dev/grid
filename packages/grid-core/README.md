# @jsoc/grid-core

Framework agnostic config generator and manager for data grid components.

## Features

- Fully framework-agnostic (React, Vue, Angular, Svelte, etc)
- Grid Schema Store: Manage multiple grid schemas and their configurations
- Dynamic Column and Row generation: Generate columns and rows directly from the JSON data.

## Installation

```bash
npm install @jsoc/grid-core
```
