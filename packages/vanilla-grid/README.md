# @jsoc/vanilla-grid

Package for generating runtime configurations for Javascript Grid Components such as AG-Grid, TanStack Table, etc. directly from a response data (JSON).

## Installation

```bash
npm install @jsoc/vanilla-grid
```
