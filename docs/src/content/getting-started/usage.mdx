import { Example } from "@/components/docs/example/Example";
import { If } from "@/components/docs/content/If";
import { Steps } from "nextra/components";
import { Snippet } from "@/components/Snippet";

# Usage

## Basic Example

Basic example to get you started.

<Steps>

### Import necessary features

<If adapterId="react-grid">
  Import the grid store creation hook and reactive selector hook:
</If>
<If adapterId="vanilla-grid">
  Import the grid store creation hook from the plugin package:
</If>
<If adapterId="vue-grid">
  Import the grid store creation hook from the plugin package:
</If>

<Snippet exampleId="basic" snippetId="import" />

### Create the store

<If adapterId="react-grid">
  Create the grid store with `useGridStore` and your grid data, then read the
  active schema using `useGridStoreSelector`:
</If>

<If adapterId="vanilla-grid">Create the grid store using `createGridStore`</If>

<If adapterId="vue-grid">
  Create the grid store with `useGridStore` and your grid data, then read the
  active schema using `useGridStoreSelector`:
</If>

<Snippet exampleId="basic" snippetId="create" />

### Render grid component

Pass the generated config to the grid component:

<Snippet exampleId="basic" snippetId="render" />

### That's all!

Your grid component is ready to use. Here is how it looks:

<br />

<Example exampleId="basic" />

</Steps>
