packages:
  - docs
  - examples/**
  - packages/**

allowBuilds:
  esbuild: true
  sharp: true # for nextra-theme-docs
  unrs-resolver: true # for nextra-theme-docs

catalog:
  tsdown: "^0.x"
  typescript: "^6.0.3"

linkWorkspacePackages: true
